# Accepted gaps (baseline)

Findings listed here are **known and accepted** — the `/uat run` loop suppresses them so each run surfaces only *new* signal (like a lint baseline). Add an entry when the user reviews a finding and decides it's out of scope or won't-fix (for now).

Format:

```
- id: <finding id or short slug>
  journey: <journey>
  why_accepted: <reason — out of scope, deferred, by-design>
  added: <YYYY-MM-DD>
```

## Entries

_(none yet — this is the pilot baseline. Populate as findings are triaged.)_
