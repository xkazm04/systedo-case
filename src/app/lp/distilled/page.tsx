import LandingDistilled from "@/components/brand/variants/LandingDistilled";

/** Landing comparison — Variant C ("distilled"). Same Monolith world and brand as
 *  the incumbent homepage; the page is stripped of what does not argue (hero
 *  lockup, eyebrow pill, kickers, PNG chips, the repeated CTA) and the room is
 *  spent on the layer the incumbent lacks: what it does, how the channels differ,
 *  and what it costs. */
export default function DistilledLandingPage() {
  return <LandingDistilled />;
}
