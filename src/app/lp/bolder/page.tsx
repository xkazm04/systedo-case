import LandingBolder from "@/components/brand/variants/LandingBolder";

/** Landing comparison — Variant A ("bolder" + "typeset"). Same facts and same
 *  Monolith world as the homepage, amplified: the onyx mass runs past the fold,
 *  Geist Mono takes the measurement role, and the four proof figures are typeset
 *  as the four different kinds of number they actually are. */
export default function BolderLandingPage() {
  return <LandingBolder />;
}
